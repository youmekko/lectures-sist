package com.test021;

//생성자 테스트 클래스
public class Sample {
	
	//생성자(Constructor)
	/*
	
	- 객체 생성시 자동 호출되는 메소드
	- 객체가 가지고 있는 멤버(필드)의 사용자 정의 초기화를 담당
	- 생성자 선언시 반환자료형이 없다. return 구문이 없다.
	- 생성자 선언시 생성자 이름은 클래스이름이다.
	- 생성자 선언시 접근제한자는 public 권장. 외부에서 생성자 접근이 안되는 경우 해당 클래스의 객체 생성도 불가.
	- 생성자 선언이 생략된 경우 기본 생성자(매개변수 목록이 없는 생성자)가 자동 생성된다.
	- 생성자는 instance 메소드이다.
	- 생성자는 모든 클래스의 필수 멤버이다.
	
	public 클래스이름() {
	}
	
	public 클래스이름(매개변수 리스트) {
	}
	
	*/
	
	//현재 클래스는 생성자가 생략된 상태이므로 기본 생성자가 자동 생성된다.
	 

}
